import { css } from '@emotion/css'

export const ShareBtnCss = css`
  border: none;
  background: transparent;
  overflow: none;
  transition: 3s all ease;

  &:hover {
    transform: scale(1.2);
    background: transparent;
  }
`
